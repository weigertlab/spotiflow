[build-system]
requires = ["setuptools<=71.0",
            "setuptools_scm[toml]>=6.2",
            "wheel",
            "numpy"]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
write_to = "spotiflow/_version.py"
